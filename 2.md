风扇曲线规则

坐标系

- X 轴：温度 0-80°C
- Y 轴：PWM 转速 0-100%
- 共 17 个控制点，每隔 5°C 一个（0, 5, 10, ... 80°C）

PWM 限制

- 最低转速：20%（任何点不得低于 20%）
- 最高转速：100%

单调性约束

曲线必须单调不递减：每个点的 PWM ≥ 左侧相邻点的 PWM。

拖动行为

用户拖动某个点时：

- 该点 PWM 被限制在 [左侧最近锚点的 PWM, 右侧最近锚点的 PWM] 之间
- 未被用户修改过的相邻点会被自动推移以维持单调性
- 被拖动的点标记为"已修改"（锚点），推移的点不标记

Tmax 限制（仅 sys 风扇）

- 当硬盘温度超过阈值（HDD: 55°C，SSD: 70°C）时，触发 Tmax 限制
- Tmax 点及其右侧所有点强制为 100%，用户不可编辑
- CPU 风扇不受此限制

默认曲线

从 0°C 到 80°C 线性插值：20% → 100%，公式：

pwm[i] = round(20 + (i / 16) \* 80) // i = 0..16

重置行为

- 无 Tmax 限制时：重置为默认线性曲线
- 有 Tmax 限制时：重置为 Tmax 对应的曲线（Tmax 点前线性插值，Tmax 点及之后为 100%）
